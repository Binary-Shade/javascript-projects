const button = document.querySelectorAll("button");
const display = document.querySelector(".display");
const specialChars = ["/", "%", "*", "+", "-", "="];
let output = "";

const calculate = () =>{
    
}